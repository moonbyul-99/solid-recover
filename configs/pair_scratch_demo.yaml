# pair_config.yaml

data:
  data_path: "/home/rsun@ZHANGroup.local/sr_project/eval_data/merge_dataset/mdata.h5mu"
  train_split_path: "/home/rsun@ZHANGroup.local/sr_project/eval_data/data_split/train_id_1.npy"
  test_split_path: "/home/rsun@ZHANGroup.local/sr_project/eval_data/data_split/test_id_1.npy"
  key_1: "rna_count"
  key_2: "peak_count"
  batch_size: 128

model:
  feature_num_1: null # 在脚本中动态确定
  feature_num_2: null # 在脚本中动态确定
  hidden_params_1: [1024, 512,  128]
  hidden_params_2: [1024, 512,  128]
  embed_dim: 64
  use_rmsnorm: true
  use_residual: true
  dropout_p: 0.0
  clip_temperature: 0.07

training:
  project_dir: "runs/sr_pair_demo"
  train_steps: 160
  eval_points: 80
  save_points: 50
  device: "cuda"

optimizer:
  lr: 1e-3
  warmup_steps: 10
  steady_1_steps: 16
  cosine_anneal_steps: 100
  min_lr: 1e-4

loss:
  vae_beta_1: 1.0
  vae_beta_2: 1.0
  clip_weight: 40.0
  cross_recon_1: 0.4
  cross_recon_2: 0.4
  temperature: 0.07